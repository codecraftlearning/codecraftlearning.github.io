import { TimestampPipePipe } from './timestamp-pipe.pipe';

describe('TimestampPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimestampPipePipe();
    expect(pipe).toBeTruthy();
  });
});
